{
  "name": "365-technologies-site",
  "version": "1.0.0",
  "private": true,
  "description": "365 Technologies — staging site (Railway). Plain Node/Express + EJS, no database, no Docker. Built to be ported into a WordPress theme later.",
  "main": "server.js",
  "scripts": {
    "start": "node server.js"
  },
  "engines": {
    "node": ">=18"
  },
  "dependencies": {
    "express": "^4.19.2",
    "ejs": "^3.1.10",
    "nodemailer": "^6.9.14"
  }
}
